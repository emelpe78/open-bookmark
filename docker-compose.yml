services:
  open-bookmark:
    build: ./open-bookmark
    ports:
      - "3778:3778"
    environment:
      DATABASE_PATH: /data/bookmarks.db
      APP_PORT: "3778"
      PORT: "3778"
      HOST: "0.0.0.0"
    volumes:
      - openbookmark_data:/data

volumes:
  openbookmark_data:
