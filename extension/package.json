{
  "name": "open-bookmark-extension",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "icons": "bash scripts/generate-icons.sh",
    "prebuild": "npm run icons",
    "dev": "vite",
    "build": "vite build",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@crxjs/vite-plugin": "^2.0.0-beta.31",
    "@types/chrome": "^0.0.320",
    "typescript": "~5.8.3",
    "vite": "^6.3.5",
    "vitest": "^3.2.4"
  }
}
